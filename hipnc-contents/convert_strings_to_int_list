int numberOfPoints = npoints(0);

for(int i = 0; i < numberOfPoints; i++)
{
    string indexConnections = point(2, "indexConnections", i); 
    printf("%s", indexConnections);
    // string connectionsList[] = split(indexConnections,",");
    
    // int num_connections = len(connectionsList);
    
    // int intList[];
    
    // for(int j = 0; j < num_connections; j++)
    // {
    //     append(intList, atoi(connectionsList[j]));
    // }
    
    // setpointattrib(0, "newConnectionList", i, intList, "set");
}